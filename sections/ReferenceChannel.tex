\section{Reference Channel Fit}
\label{sec:ReferenceChannel}


\subsection{Background Composition}
\label{subsec:refBackground}



\subsection{Fit Procedure}
\label{subsec:refFit_procedure}

The reference channel yield is extracted using an unbinned maximum likelihood fit in the mass range 4930 MeV to 5630 MeV. This section describes the fit procedure and the pdfs used for the reference channel and each of the background components.

The data pdf is the sum of the reference channel signal and four background contributions: $B^{\pm}\to J\psi K^\pm$, $B^{\pm}\to J\psi \pi^\pm$, PRD1, PRD2, and combinatorial. The decays from PRD3 are not included in the data fit because their flat distribution is heavily correlated with the combinatorial background. The exlusion of PRD3 is treated as a systematic. 

The PDFs used in the fit are the following:
\begin{description}
\item [$B^{\pm}\to J\psi K^\pm$] This fit is performed using the sum of two Johnson SU pdfs. The paramaterization is the following: 
\begin{equation}
    PDF = \frac{\delta}{\lambda \sqrt{2\pi}} \frac{1}{\sqrt{1+(\frac{x-\mu}{\lambda})^2}} exp[-\frac{1}{2}(\gamma + \delta \sinh ^{-1}(\frac{x-\mu}{\lambda}))^2]
\end{equation}

\item[$B^{\pm}\to J\psi \pi^\pm$] This shape is modeled using the sum of a Johnson Su function and a gaussian. 

\item[PRD1] This shape is modeled using a sigmoid. The sigmoid used is paramaterized as follows
\begin{equation}
    1-\frac{x}{\sqrt(1+x^2)}% 1-(x/TMath::Sqrt(1+ x*x))
\end{equation}
where $x = \sigma_PRD1 (m-\mu_PRD1)$. The PRD1 shape falls off around 5150 MeV. Many standard step functions like the error function and fermi dirac function required an additional exponential component to model the complex curvature without significant pulls. This choice of sigmoid accurately models the steep drop off at 5150 MeV and the tail resembeling an exponential after 5200 MeV with only two parameters, which significantly improves the fit convergence and simplifies systematic studies on the PRD composition. 

\item[PRD2] An ExpGaus \cite{expgauss} function is chosed to model PRD2. PRD2 has a central peak that appears gaussian but an extended tail. Typically this type of distribution is fit using a Crystal ball function, however, the crystal ball parameters are unweildy and the PRD2 is a small component in the fit. Therefore, the simpler ExpGaus model was used. The ExpGau model is paramaterized as:
\begin{equation}
    \begin{cases}
        exp(-0.5(\frac{m-\mu}{\sigma})^2) & \text{if } m <= \mu-k\sigma \\
        exp(\frac{k^2}{2} + k\frac{m-\mu}{\sigma}) & \text{if } m > \mu-k\sigma \\
    \end{cases}
\end{equation}
% RooGenericPdf PRD2_ExpGaus_MC_NotExtended("PRD2_ExpGaus_MC_NotExtended","exp(-0.5*( ((mass_var-PRD2_mu_2)/PRD2_sigma_2)^2 )) * (mass_var<=PRD2_mu_2-PRD2_k*PRD2_sigma_2) + exp( 0.5*PRD2_k^2 + PRD2_k*((mass_var-PRD2_mu_2)/PRD2_sigma_2) )* (mass_var>PRD2_mu_2-PRD2_k*PRD2_sigma_2)",RooArgSet(mass_var,PRD2_mu_2,PRD2_sigma_2,PRD2_k)) ;

\item [Combinatorial] The combinatorial background spans the entire mass window. It's occurance decreases towards higher mass. A second order Chebyshev polynomial is used to for its shape. 
\end{description}

The fit to data is performed simultaneously with the MC sub-samples to constrain the background models. An identical PDF is used to fit the corresponding MC distribution except for the yield, which is a free parameter in the data fit. The MC sub-samples used are the $B^{\pm}\to J\psi K^\pm$, $B^{\pm}\to J\psi \pi^\pm$, PRD1, and PRD2. The only shape not constrained by MC is the combinatorial background. A gaussian convolution with a common mean and standard deviation is applied to the MC shapes to account for discrepancies between the MC and detector resolution. 

The fitting procedure is done in 5 steps. The first step is fitting the MC alone. The MC fits all use a single PDF and are robust to changing initial conditions. This sets the initial conditions for the shape parameters in the remaining steps on data. The second step is a simultaneous fit with data and MC. For this step, the yeild of each fit component is set to the partial run 2 fraction scaled to the full run 2 dataset yield. The systematic from this choice is accounted for in the closure test. The smearing gaussian and combinatorial shape parameter initial condition are chosen to resemble the partial run 2 values. This choice is treated as a systematic in the next section. For the second step, the shape parameters are kept frozen to their MC values. This allows the values not constrained be the MC to find reasonable initial values before the fit reaches its maximal complexity. In the remaining steps (steps 3-5), the shape parameters are released based on their size in the fit and their proximity to the signal. In step 3, all parameters of the $B^{\pm}\to J\psi K^\pm$ are released. In step 4, all parameters of the $B^{\pm}\to J\psi \pi^\pm$ are released. Finally, in step 5 the PRD1 and PRD2 shape parameters are released. 

In each step of the fit, a high quality migrad status is required. If this is not acheives, the fit is re-ran with a more robust migrad algorithm. The hesse algorithm is used to compute the parameter errors, except for the $B^{\pm}\to J\psi K^\pm$ yield, which uses the minos algorithm. 


\subsection{Systematics}
\label{subsec:refFit_systematics}

This section details the systematics studied associated with choices made in the reference channel fit. These choices are devided into two categories: (1) choices pertaining to the PDFs used and (2) choices pertaining to the MC. The systematics in the first category are:
\begin{description}
    \item[Choice of $B^{\pm}\to J\psi K^\pm$ PDF] Several different models were evaluated on the $B^{\pm}\to J\psi K^\pm$ MC. The best performing model, based on the $\chi^2$ test, is two JohnsonSu functions. The second best model was a JohnsonSu plus a gaussian. These models are shown in Figure \ref{fig:RefCh-Alt-BK-MC} with the $B^{\pm}\to J\psi K^\pm$ MC. For the default fit, the double JohnsonSu model is used. To evaluate the systematic in this choice, a dedicated fit to the data is made with a Johnson plus Gaussian model. The difference in the yields 

    \item[Choice of $B^{\pm}\to J\psi \pi^\pm$ PDF] Fits with alternate models on the $B^{\pm}\to J\psi \pi^\pm$ MC are shown in Figure \ref{fig:RefCh-Alt-BPi-MC}, the best performing model is again the JohnsonSu + JohnsonSu, but it is only marginally better than the simpler JohnsonSu + Gaussian. Since the performance of both models is similar, the simpler Johnson+Gaussian model is opted for in the default fit. To evaluate the systematic, a dedicated fit is made with the Johnson+Johnson model.


    \item[Choice of PRD1 PDF] Fits with a sigmoid and FermiDirac+exponential model on the PRD1 MC are shown in Figure \ref{fig:RefCh-Alt-PRD1-MC}. The sigmoid has lower pulls through the entire mass range, and does so with only two parameters as opposed to the Fermi-Dirac plus exponential which uses 4 parameters. The sigmoid is chosen as the default PDF, and a dedicated fit using the fermi-dirac + exponential is made to evaluate the systematic associated with this choice. 
    
    \item[Choice of PRD2 PDF] Fits with alternate models on the PRD2 MC are shown in Figure \ref{fig:RefCh-Alt-PRD2-MC}. Although the pull plot shows very similar performance between the two models, the ExpGaus acheives this fit with less parameters, and therefore has a lower $\chi^2$. The ExpGaus is chosen as the default pdf, and a dedicated fit is performed with the Crystal ball function to evaluate the systematic associated with this choice. 

    \item [Choice of Combinatorial PDF] The combinatorial is not constrained by MC, so the best performing PDF is determined by its $\chi^2$ on data. Two shapes are used, an exponential and a second order chebychev polynomial. The Chebychev fit has lower $\chi^2$ when applied to the data and is therefore chosen as the default PDF. To evaluate the systematic associated with this choice, a dedicated fit is executed using an exponential as the combinatorial pdf.

\end{description}


\begin{figure}[h]
    \centering
    \includegraphics[width=0.6\textwidth]{figures/ReferenceChannel/250206-BK-JJ-JG-log.pdf}
    \caption{Fits to the $B^{\pm}\to J\psi K^\pm$ MC with alternate PDFs.}
    \label{fig:RefCh-Alt-BK-MC}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.6\textwidth]{figures/ReferenceChannel/250222-MCfits-BPi.pdf}
    \caption{Fits to the $B^{\pm}\to J\psi \pi^\pm$ MC with alternate PDFs.}
    \label{fig:RefCh-Alt-BPi-MC}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.6\textwidth]{figures/ReferenceChannel/250206-PRD1-Sigmoid-ErrorExp.pdf}
    \caption{Fits to the PRD1 MC with alternate PDFs.}
    \label{fig:RefCh-Alt-PRD1-MC}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.6\textwidth]{figures/ReferenceChannel/250206-PRD2-CB-ExpGaus.pdf}
    \caption{Fits to the PRD2 MC with alternate PDFs.}
    \label{fig:RefCh-Alt-PRD2-MC}
\end{figure}


The systematics in the second category are 
\begin{description}
    \item [Charge asymmetry in the MC] The data fit uses events with both a positive hadron and a negative hadron. The fraction of positive hadrons in a sample is defined as $f_+=\frac{N_+}{N_++N_-}$ where $N_+$ is the number of samples with a positively charged hadron and $N_-$ is the number of samples with a negative hadron. The data was separated into two categories based on the hadron charge and fit. Fron these fits, it was determined the $f_+=50.6\%$ in data. This contradicts the MC prediction of$f_+=48.6\%$. To evaluate the systematic induced by this difference, the MC was reweighted to represent different values of $f_+$. Care was taken to ensure the total weighted number of MC samples remained constant, since reducing the MC sample size effects the fit result. The fit was performed with  $f_+ = 0,48.6,50.6,52.6,100 \%$. The result of the fit is shown in Figure \ref{fig:RefCh-ChargeAsymmetry}.

    \item[Kinematic Reweighting] To asses the systematic induced by the DDW and QLC weights on the MC, a dedicated fit is performed without both of these weights on the MC. The difference in the yield is taken as the systematic error.
    
    \item[PRD Composition] The PRD is generated based on a list of decays with their PDG decay rates. In many cases, the error on the PDG decay rate exceeds the statistical error expected in the sample, and therefore the composition of the PRD sample might be biased. To evaluate this systematic uncertainty, the error on the major PRDs represented in the PRD1 and PRD2 samples are calculated. Then, each channel in the PRD is bootstrapped to a new number of events. The new number is the original number times a statistical fluctuation from a poisson distribution times a random number based on the PDG uncertainty on that channels branching ratio. This bootstrapping procudure is repeated 1000 times, each time the boostrapped PRD dataset is fit and the parameters are saved. The correlation between the paramters for PRD1 and PRD2 are shown in figure \ref{fig:}. Then, the distributions of the fit parameters is used to create a correlation matrix between the PRD shape parameters. This matrix is passed into the full data fit instead of performing the fit simultaneously with the PRD samples. The correlation matrix contains information about the variance in the fit parameters from the bootstrapping, and thus this PRD constrained fit is an evaluation of our systematic uncertainty from the PRD composition. 

\end{description}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.6\textwidth]{figures/ReferenceChannel/250530-ChargeAsymmetry.pdf}
    \caption{The reference channel yield with different fractions of positive hadron data in the $B^{\pm}\to J\psi K^\pm$ MC. }
    \label{fig:RefCh-ChargeAsymmetry}
\end{figure}

% \begin{figure}[h]
%     \centering
%     \includegraphics[width=0.6\textwidth]{figures/ReferenceChannel/250530-PRDcomposition.pdf}
%     \caption{The shape parameters in the PRD1 (left) and PRD2 (right) datasets when fit to samples bootstrapped based on the PDG branching ratio errors.}
%     \label{fig:RefCh-ChargeAsymmetry}
% \end{figure}



Each of these systematics is evaluated on a fit to data. Plots showing these fits are saved in Appendix.... The yields from these fits are summarized in Figure \ref{fig:RefCh-AllSystematics}. 


\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/ReferenceChannel/250530-systematics.pdf}
    \caption{Yield values from all systematics studied.}
    \label{fig:RefCh-AllSystematics}
\end{figure}



\subsection{Closure Test}
\label{subsec:refClosure}

A closure test is developed to evaluate if the fit procedure can reproduce the yield with different underlying fractions of PDF. The final fit result using the default setup is taken. Each yield is veried randomly according to a poisson distribution centered on the fit value. Then pseudo-data is generated using the fit shapes and the varied yield. An identical fit procedure as applied to the data is applied to the pseudo-data and the fit yield is compared with the true yield. The results...

\subsection{Yield Result}
\label{subsec:refFit}